[dev-dependencies]
dotenv = "0.15"
reqwest = "0.11"
sqlx = { version = "0.5", features = [
    "runtime-tokio-rustls",
    "postgres",
    "macros",
    "chrono",
    "uuid",
] }
uuid = { version = "0.8", features = ["serde", "v4"] }